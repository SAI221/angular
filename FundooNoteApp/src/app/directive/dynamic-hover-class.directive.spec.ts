import { DynamicHoverClassDirective } from './dynamic-hover-class.directive';

describe('DynamicHoverClassDirective', () => {
  it('should create an instance', () => {
    const directive = new DynamicHoverClassDirective();
    expect(directive).toBeTruthy();
  });
});
